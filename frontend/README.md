# Frontend del TFG - Predicción de Partidas LoL

Para arrancar el entorno de desarrollo:
 `npm run dev`

Para correr los tests unitarios:
 `npx vitest`

Para correr los test de interfaz:
 `npx playwright test --ui`
